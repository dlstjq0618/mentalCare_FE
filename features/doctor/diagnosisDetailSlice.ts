import { createSlice, PayloadAction } from "@reduxjs/toolkit";

export interface DiagnosisDetailState {}

const initialState: DiagnosisDetailState = {};

export const diagnosisSlice = createSlice({
  name: "diagnosisDetail",
  initialState,
  reducers: {},
});
